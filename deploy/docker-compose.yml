services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    # ports:
    #   - "8501:8501"
    expose:
      - "8501"
    volumes:
      - ratings_data:/app/ratings
    restart: unless-stopped

volumes:
  ratings_data:
